---
const { tags = [] } = Astro.props;
---

{tags.length > 0 && (
  <div class="flex flex-wrap gap-2">
    {tags.map((tag) => <span class="rounded-sm border border-zinc-200 px-2 py-1 text-xs text-zinc-600">#{tag}</span>)}
  </div>
)}
